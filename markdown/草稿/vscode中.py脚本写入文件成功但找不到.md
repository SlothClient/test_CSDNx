vscode中运行代码，终端默认在你打开的工作区的路径下，py脚本中写文件使用相对路径时，脚本没有在脚本文件当前路径的终端中运行，并且你写了`os.makedirs("xxx", exist_ok=True)`时，不会报错，文件会直接写到你运行终端的路径相对应的相对路径中去，你没有在期望的地方看到，故以为执行成功但没文件夹/文件生成

而使用pycharm时，你运行会默认在脚本文件当前路径运行，这一点如果你换软件需要格外注意！！
